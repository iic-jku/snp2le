# Screenshots

Place the images referenced by the root [`README.md`](../../README.md) here:

| File | Suggested content |
| --- | --- |
| `snp2le_gui_bpf.png` | The GUI converting the BPF example (Design & Schematic view). |
| `snp2le_plots_bpf.png` | The Plot view: data (grey) vs model (blue) vs imported simulation (red). |

PNG, exported at a comfortable width (the README scales them to ~85%). Add more as needed and link them from the README.
